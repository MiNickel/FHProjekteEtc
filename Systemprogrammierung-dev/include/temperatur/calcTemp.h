/*!
 * \file calcTemp.h
 * \brief Definitionen für calcTemp
 */
 
/**
 * \fn float calculateTemperature(int raw) berechnet die Innentemperatur
 * \param int raw ist der Wert in mv, aus dem die Temperatur berechnet wird
 * 
 */
float calculateTemperature(int raw);
